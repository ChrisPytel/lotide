//"head.js A6 - Added comments"

//Head takes in an array as a parameter, and returns the first value found in it.
const head = function(array) {
  return array[0];
};

module.exports = head; //exports our assertEqual function to communicate across modules